import express, {Request, Response} from "express";

export default class BotService {
    public async handleWhatsAppMetaCallback(req: Request, res: Response): Promise<void> {
        try{

        }catch (error) {
            
        }
    }
}